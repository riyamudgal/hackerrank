#include <stdio.h>
int factorial(int n)
   {
     
       if(n>0)
            return n*factorial(n-1);
        else 
            return 1;
   }
   int main()
   {
       int f,n;
       scanf("%d",&n);
       f=factorial(n);
       printf("%d", f);
       return 0;
   }
